/*Program to find product in float and int*/
#include<stdio.h>
int main()
{
	float a, b, c;
	scanf("%f %f", &a, &b);
	c=a*b;
	printf("The product in float is %f \n", c);
	printf("The product in integer is %d", c);
	return 0;
}
